{
  "name": "internote",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "dev": "npx concurrently \"yarn --cwd ui dev\" \"yarn --cwd api dev\"",
    "serverless:deploy": "yarn serverless:deploy:api",
    "serverless:deploy:api": "yarn --cwd api serverless:deploy",
    "serverless:local": "npx concurrently \"yarn --cwd api serverless:local\""
  },
  "license": "ISC",
  "workspaces": {
    "packages": [
      "api",
      "ui"
    ],
    "nohoist": [
      "ts-node-dev",
      "ts-node"
    ]
  }
}
