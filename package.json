{
  "name": "internote",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "dev": "npx concurrently \"yarn --cwd ui dev\" \"yarn --cwd api dev\"",
    "dev:docker": "docker-compose up ui-dev api-dev",
    "dev:docker:rebuild": "docker-compose up --build ui-dev api-dev"
  },
  "license": "ISC",
  "workspaces": {
    "packages": [
      "api",
      "ui"
    ],
    "nohoist": [
      "ts-node-dev",
      "ts-node"
    ]
  }
}
