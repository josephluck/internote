{
  "name": "internote",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "dev": "npx concurrently \"yarn --cwd ui dev\" \"yarn --cwd api dev\"",
    "deploy:dev": "npx concurrently \"yarn deploy:dev:api\"  \"yarn deploy:dev:ui\"",
    "deploy:dev:api": "yarn --cwd api deploy:dev",
    "deploy:dev:ui": "yarn --cwd ui deploy:dev",
    "deploy:prod": "npx concurrently \"yarn deploy:prod:api\"  \"yarn deploy:prod:ui\"",
    "deploy:prod:api": "yarn --cwd api deploy:prod",
    "deploy:prod:ui": "yarn --cwd ui deploy:prod"
  },
  "license": "ISC",
  "workspaces": {
    "packages": [
      "api",
      "ui"
    ],
    "nohoist": [
      "**"
    ]
  }
}
