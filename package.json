{
  "name": "internote",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "dev": "npx concurrently \"yarn --cwd ui dev\" \"yarn --cwd api dev\"",
    "deploy": "npx concurrently \"yarn deploy:api\"  \"yarn deploy:ui\"",
    "deploy:api": "yarn --cwd api deploy",
    "deploy:ui": "yarn --cwd ui deploy"
  },
  "license": "ISC",
  "workspaces": {
    "packages": [
      "api",
      "ui"
    ],
    "nohoist": [
      "**"
    ]
  }
}
